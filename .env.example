# Example .env for YKS Takip & Koçluk (Vite)
# Copy to .env and fill in your values.

# Firebase Cloud Messaging Web Push (VAPID) public key
VITE_FIREBASE_VAPID_KEY=BKx1_your_web_push_public_key_from_firebase_console

# If you also use generic Web Push (non-FCM), reuse the same key or your own VAPID key
VITE_VAPID_PUBLIC_KEY=BKx1_same_or_custom_vapid_public_key
